# Required: MongoDB connection URI (mongodb:// or mongodb+srv://).
MONGO_URI=
# Required: a randomly generated signing secret of at least 32 characters.
JWT_SECRET=

# Optional: TCP port; defaults to 3000.
PORT=
# Optional: comma-separated HTTP(S) origins, no paths or trailing slashes.
# Blank disables browser cross-origin access.
CORS_ORIGIN=

# Optional Google OAuth: set all four together, or leave all blank to disable.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Full backend callback URL ending in /auth/google/callback.
GOOGLE_CALLBACK_URL=
# Frontend HTTP(S) origin; callback redirects to /auth/sign-in.
FRONT_URI=

# Optional S3: bucket and region required when enabling storage.
AWS_BUCKET_NAME=
AWS_REGION=
# Optional credential pair; blank uses the AWS SDK default credential chain (IAM roles).
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Optional session token for temporary credentials; requires the credential pair above.
AWS_SESSION_TOKEN=
# Optional public CDN base URL; required only when calling AwsS3Service.getFile.
CLOUD_FRONT_URL=
